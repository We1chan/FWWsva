# 验收点 2–4：前后端业务组说明

## 基线（Phase 0）

**记录时间：** 2026-09-02 01:00:43 +08:00。

### 源码状态

| 仓库 | 分支 | 提交 | 工作区状态 |
| --- | --- | --- | --- |
| `FWWsva` | `codex/frontend-backend-acceptance` | `4ac77e3de30b5794141798894e593dcb63988cfa` | 已有未跟踪的验收截图、计划和设计稿，未修改、未暂存。 |
| `SVA-backend` | `codex/frontend-backend-acceptance` | `f3f90360ac35581487869b3c8e8f99aa8fc2155c` | 干净。 |
| `SVA-mediaServer` | `codex/frontend-backend-acceptance` | `daa9f4819d0285262755109b98db78220c6d19f0` | 干净。 |
| `SVA-web` | `codex/frontend-backend-acceptance` | `cb1230b2235cc2d5b20c6c079e15baf07cd6c224` | 干净。 |

`SVA-server` 保持在 `master`（`cccfcd3960e2215d15f84adb19aa3b47f738c630`），其既有未跟踪目录 `build-gpu/` 未触碰。

### 服务与 HTTP 检查

在 WSL `Ubuntu-22.04-easySVA` 执行：

```bash
systemctl is-active easysva-backend easysva-media easysva-analyzer \
  easysva-rtsp-simulator nginx mariadb redis-server
```

七项结果均为 `active`。Windows 侧执行：

```powershell
curl.exe -sS -o NUL -w "web=%{http_code}`n" http://localhost/
curl.exe -sS -o NUL -w "captcha=%{http_code}`n" http://localhost/prod-api/captchaImage
curl.exe -sS -o NUL -w "zlm=%{http_code}`n" http://localhost:9992/index/api/getMediaList
```

结果分别为 `web=200`、`captcha=200`、`zlm=200`。未登录的 ZLM `getMediaList` 返回业务码 `-100` 和 `Please login first`，因此仅证明服务可达，不能替代已认证的流列表校验。

### RTSP、YOLO 与告警数据

从 `easySVA.h_device` 查询到：

| ape_id | 名称 | 来源 | 状态 | 直连地址 | 播放地址 |
| --- | --- | --- | --- | --- | --- |
| `dev_mock_camera_001` | 模拟RTSP摄像头 | `DIRECT` | `RUNNING` | `rtsp://127.0.0.1:9994/live/mock-camera` | `rtsp://127.0.0.1:9994/live/mock-camera` |

从 `easySVA.h_waring` 按 `alarm_time DESC` 查询到最近五条均为 `停留告警`，设备均为 `dev_mock_camera_001`；最新记录时间为 `2026-09-01 20:06:00`，并含 `picture_absolute_url`。

已有页面/流画面取证（均为本阶段前已存在的未跟踪文件，未纳入本次暂存）：

- `docs/acceptance-point1-source-frame.jpg`
- `docs/acceptance-point1-person-source.jpg`
- `docs/acceptance-point1-overlay-preview.jpg`
- `docs/acceptance-point1-real-alarm.jpg`

### 测试与已知阻塞

| 检查 | 命令 | 结果 |
| --- | --- | --- |
| 后端回归 | `mvn -pl ruoyi-admin -am test`（WSL） | 测试在 `ruoyi-common` 的 `maven-compiler-plugin` 编译 110 个源文件时长期无进展，未生成 `surefire-reports`；已中止，需在后续阶段排查编译卡滞。 |
| 前端依赖 | `npm ci` | 成功，npm 日志以 `exit 0` 结束。 |
| 前端静态检查 | `npm run lint` | 失败：ESLint 7.15.0 报告传入的 `src` 被整体忽略。根因是现有 `.eslintignore` 的 `src/assets` 规则被 ESLint 解析为目录 `src`，本阶段未修改该既有配置。 |
| 告警截图 URL | `curl http://localhost/alarm/task_yolo_dwell_001/detect_event_1788264390191_74577462.jpg` | HTTP 404；数据库记录仍存在。需在后续验收前核实 Nginx 静态路径或告警文件留存情况。 |

因此，本基线已证明核心服务、登录入口、模拟 RTSP 设备状态及历史 YOLO 告警记录存在；但页面登录后的实际预览、已认证 ZLM 流清单、告警图片 HTTP 可访问性及两项自动化检查尚不能判定通过。

### 2026-09-02 Phase 0 阻塞复验

- 已更正上表中 `SVA-backend` 的基线提交号为实际 `f3f90360ac35581487869b3c8e8f99aa8fc2155c`。
- 首次验证码 502 的直接原因是 Nginx 上游 `127.0.0.1:9114` 在 WSL 自动退出、下次命令重新启动后端的启动窗口内拒绝连接；运行仓库既有 `deploy/scripts/start-wsl-keepalive.ps1` 后，本机 WSL 内 `GET /prod-api/captchaImage` 返回 HTTP 200。后端、Nginx、MariaDB、Redis、MediaServer、Analyzer 和 RTSP 模拟器均为 `active`。
- 通过 MariaDB TCP 认证只读复验，`h_device` 有 2 行、`h_waring` 有 79 行；未认证的 ZLM `getMediaList` HTTP 为 200。管理员登录、带 Token 的设备/告警 API 与带 ZLM secret 的流列表仍需在后续稳定会话中补取，不能以本条 HTTP 可达性代替。
- 告警截图 404 已定位为数据留存问题而非 Nginx 路由问题：Nginx 的 `/alarm/` alias 正确指向 `/var/www/SVA-web/upload/alarm/`，但数据库所引用的 `detect_event_1788264390191_74577462.jpg` 文件不存在；同目录还存在两个 0 字节告警文件。不会伪造或复制截图文件，需由告警生产/留存链路补齐有效原图后再验收。
- `mvn -pl ruoyi-admin -am test` 的编译卡滞、以及 `npm run lint` 因 `.eslintignore` 将 `src` 整体忽略的失败，仍保留为未解决基线门禁；本次未改动构建或 ESLint 配置。

### 2026-09-02 阶段 1：数据库与后端契约交付记录

**执行时间：** 2026-09-02 16:20–16:40 +08:00。

- 迁移脚本 `deploy/sql/20260901_gb28181_business.sql`：创建 `gb28181_channel`（`(platform_id,device_id,channel_id)` 唯一，含显式 `zlm_server_id/vhost/app/stream` 绑定与目录/媒体在线状态）；为 `h_device` 幂等补充 `device_type/gb_device_id/gb_platform_id/gb_channel_id/last_seen_at/sync_source` 列与 `uk_h_device_ape_id` 唯一键；幂等插入 `h_waring_type` 的 `SLEEP_DUTY/睡岗告警`。
- 迁移验证：对现有 `easySVA` 库与新建空库（仅含旧结构 `h_device/h_waring_type`）各连续执行两次均成功，幂等无报错；空库断言 `gb28181_channel` 建表、列补充、唯一键、`SLEEP_DUTY` 插入正确，临时验证库已删除。
- 后端模型与持久化：`HDevice` 增加设备类型与 GB 镜像字段；新增 `Gb28181Channel`（SIP/GB 目录身份，非权威）、`Gb28181MediaStream`（无设备身份的 ZLM 媒体投影）、`Gb28181CatalogMapper(+xml)`、`Gb28181DeviceSyncService(+Impl)`（目录幂等写入 + 经 `ZlmServer.host/secret` 调 ZLM `getMediaList` 仅按显式 `vhost/app/stream` 刷新媒体可用性，缺流只标不可预览）；`HDeviceMapper(+xml)` 增加 `selectGbDevice/upsertGbDevice/markGbDevicesOffline` 与 `device_type` 筛选，所有列表/插入/更新 SQL 均含新字段。
- 测试：`Gb28181DeviceSyncServiceTest` 断言目录 `(platformId,deviceId,channelId)` 幂等落库、ZLM 仅更新显式绑定、缺流保留目录身份与在线状态（2 例通过）；`HDeviceServiceImplTest` 断言 DIRECT 设备预览 URL 拼装（1 例通过）。`mvn -pl ruoyi-admin -am test` BUILD SUCCESS，Tests run: 3 全过；此前记录的“编译卡滞”在本环境复跑不再出现。
- 部署与回归：`backend.jar` 已重新打包部署并重启后端；管理员登录 200，`/waring/device/list` code 200（2 台 RTSP 设备，`device_type=RTSP`、状态与在线标志正常），`/waring/type/list` code 200 且含 `SLEEP_DUTY | 睡岗告警`，`/waring/waring/list` code 200 total 79。RTSP 设备与原告警数据无回归。
- 提交与推送：`FWWsva` `809504d`（docs: record frontend backend acceptance baseline，补阶段 0 未提交文档）、`131da30`（feat: add GB28181 business SQL migration）；`SVA-backend` `d0a8589`（feat: add GB28181 device business model）。均已推送 `origin/codex/frontend-backend-acceptance`（经本机 127.0.0.1:7890 代理）。
- 遗留：`npm run lint`（ESLint `.eslintignore` 既有配置）与告警截图 404（告警文件留存链路）仍为未决基线门禁，属后续阶段范围。

### 2026-09-02 阶段 2：设备同步、状态和预览接口交付记录

**执行时间：** 2026-09-02 16:35–16:50 +08:00。

- 后端改动（`SVA-backend` `codex/frontend-backend-acceptance`）：
  - `HDeviceController` 新增仅管理员可调用的 `POST /waring/device/gb28181/catalog/sync`（`zlmServerId` 默认 1，`@RequestBody(required=false)` 接收目录通道快照）与 `POST /waring/device/gb28181/status/refresh`（仅按显式媒体绑定刷新），返回 `{created,updated,offlineMarked}` 与 `{available,unavailable}`。
  - `Gb28181DeviceSyncService(+Impl).syncDevices`：校验 ZLM 启用与通道 `zlmServerId` 一致性；对目录中已消失的通道按 `(zlm_server_id,gb_device_id,gb_channel_id)` 联动 `h_device` 置离线；对快照内通道幂等 upsert `gb28181_channel` 并按 `ape_id="gb-<deviceId>-<channelId>"` 镜像进 `h_device`（`device_type=GB28181`、`stream_source_type=PLATFORM`、`sync_source=GB28181目录`、保留既有 `monitor_status`）。
  - `HDeviceMapper(+xml).updateGbDeviceOnlineByChannel`：按显式绑定更新 `is_online`；`refreshMediaFromZlm` 命中/缺失流时联动该更新。
  - `HDeviceServiceImpl` GB28181 监控分流：`startMonitor` 离线抛「国标设备当前离线，无法启动监控」、无 `play_url` 抛「国标设备暂无可用播放地址，请先执行目录同步」，否则仅更新 `RUNNING`（不调用 `addStreamProxy`）；`stopMonitor` 仅置 `STOPPED`（不删 RTSP 代理）；`previewMonitor` 校验在线与可播地址后返回 `playUrl`；RTSP 分支逻辑字节级保持。新增 `@Scheduled(fixedDelayString="${easysva.gb28181.status-refresh-ms:60000}")` 定时任务，遍历启用 ZLM 节点刷新，异常仅 `log.error`，绝不动 RTSP 设备。
- 测试：`Gb28181DeviceSyncServiceTest` 6 例（目录身份保留、显式绑定匹配、ZLM 成功/空列表/连接失败、同步镜像建改+剔除离线）+ `HDeviceControllerTest` 3 例（同步统计返回、刷新统计返回、ZLM 不可达错误传播）+ 阶段 1 的 `HDeviceServiceImplTest` 1 例；`mvn -pl ruoyi-admin -am test` BUILD SUCCESS，Tests run: 10 全过。修复 1 处测试夹具缺陷（镜像 seed 缺少 `gb_device_id/gb_channel_id` 导致误判 created=2）。
- 端到端证据（WSL `Ubuntu-22.04-easySVA`，脚本与响应存 `FWWsva/docs/evidence-phase2/`）：
  1. `01-sync-create`：同步 2 个国标通道（cam-01 绑定真实流 `dev_mock_camera_001`、cam-02 绑定幽灵流）→ `{"offlineMarked":0,"created":2,"updated":0}`；`02/03` DB 取证两行通道与两行镜像（`PLATFORM`/`GB28181目录`，`is_online=1`）。
  2. `04-refresh` 状态刷新 → `{"unavailable":1,"available":1}`；`05/06` DB 取证 cam-01 `is_online=1`/`media_online=1`（`last_media_seen_at` 已更新），cam-02 `is_online=2`/`media_online=0`。
  3. `07-preview-online` cam-01 返回 `playUrl=ws://127.0.0.1:9992/live/dev_mock_camera_001.live.flv`；`08-preview-offline` cam-02 报 code 500「国标设备当前离线，无法预览」。
  4. `09-start-online` cam-01 启监控 `success:true`（无 `direct_source_url` 仍成功，证明未走 RTSP 代理）；`16-zlm-no-new-proxy` ZLM `getMediaList` 仍仅 `dev_mock_camera_001` 一条，无新增代理流；`10-start-offline` cam-02 启监控 `success:false`「国标设备当前离线，无法启动监控」；`11-stop-online` cam-01 停监控 `success:true`。
  5. `12-sync-drop` 仅上报 cam-01 → `{"offlineMarked":1,"created":0,"updated":1}`，cam-02 镜像置离线而 `gb28181_channel` 目录行保留（身份不删除）。
  6. `13-list-all` total=4（RTSP `dev_mock_camera_001` RUNNING、`demo-park-gate-01` STOPPED + GB28181 两台）证明 RTSP 无回归；`14-list-gb28181` 按 `device_type=GB28181` 过滤 total=2。
  7. `15-scheduler-recovery` 定时刷新自动恢复：16:45:52 手工将 cam-01 翻为离线（`is_online=2`、`media_online=0`、`last_media_seen_at=NULL`），未做任何手工刷新，16:46:32（下一 60s 周期）`@Scheduled` 自动从 ZLM 恢复为 `is_online=1`/`media_online=1`/`last_media_seen_at=16:46:32`。
- 部署：`backend.jar` 重新打包部署并重启 `easysva-backend`，登录与全部接口 200。
- 提交与推送：`SVA-backend` `ddaaff0`（feat: sync GB28181 devices and stream status）；`FWWsva` `8c1c636`（docs: record GB28181 phase-2 E2E evidence，含本记录与证据文件）。均已推送 `origin/codex/frontend-backend-acceptance`（经本机 127.0.0.1:7890 代理）。
- 遗留：`npm run lint` 与告警截图 404 仍为未决基线门禁；`device_type=GB28181` 的设备通过 `/list?device_type=GB28181` 查询参数筛选（沿用 HDevice 实体属性绑定，前端阶段将按此约定接入）。

### 2026-09-02 阶段 3：Vue 设备管理与预览适配交付记录

**执行时间：** 2026-09-02 17:17–18:13 +08:00。

- 前端改动（`SVA-web` `codex/frontend-backend-acceptance`，新功能分支以 master `cb1230b` 为基线）：
  - `src/api/device.js`：新增 `syncGb28181Devices(zlmServerId=1)` 与 `refreshGb28181Status(zlmServerId=1)`，调用 `POST /waring/device/gb28181/catalog/sync` 与 `POST /waring/device/gb28181/status/refresh`，`timeout: 30000`。
  - `views/device/manage.vue`：
    - 查询区新增"接入类型"下拉（label="接入类型"，选项 RTSP/GB28181，`@change=handleQueryTypeChange` → `handleQuery`）。
    - 工具栏新增"同步国标设备"按钮（el-button type=warning，`v-hasPermi="['waring:device:add']"`，`:loading=syncing`，`@click=handleSyncGb28181`）。
    - 表格列调整：原"设备类型"（`stream_source_type`）改名"媒体源"，并在其前新增"接入类型"（`device_type`）列（GB28181→warning tag=国标，其他→info tag=RTSP）。`formatDeviceType`/`formatDeviceTypeTag` 已加。
    - 对话框：width 620px→700px；首行改为两列"接入类型"（GB28181 时禁用 select）/"媒体源"（GB28181 时禁用 select）；GB28181 时新增"**国标信息（目录同步获取，只读）**"区块含 `gb_platform_id/gb_device_id/gb_channel_id/play_url` 四个 disabled 字段。视频流地址输入框 v-if 改为 `stream_source_type==='DIRECT' && device_type!=='GB28181'`。`handleUpdate` 归一化 `device_type` 大写、`is_online` 字符串化以匹配下拉选项。
    - `submitForm` 抽 `buildSubmitPayload()`：GB28181 时 `delete payload.direct_source_url`（后端 `HDeviceMapper.xml` 的 `updateDevice` 动态 `<if>` 缺省键保留原值，绝不覆盖 RTSP 直连地址）。同步异常提示统一前缀"国标设备同步失败：…"。
  - `views/device/realtime.vue`：查询区与表格新增"接入类型"列/筛选；`resetQuery` 重置 `device_type=undefined`、`monitor_status='RUNNING'`。
  - `views/device/lixian.vue`：表格新增"接入类型"列（与 manage 一致的 tag 渲染）。
  - `views/deployment/add.vue`：设备下拉 `el-option` 加 `:label` 复合渲染——类型 tag（GB28181=warning/RTSP=info）+ 在线 tag（`is_online` 1=success 启用/2=info 离线/0=primary 登录中/9=danger 其他异常）+ 名称/编码。`loadDeviceOptions` 同步保留 `raw` 字段。
  - 测试基建：`package.json` 新增 `test:unit` 脚本与 5 个 devDeps（`@vue/cli-plugin-unit-jest@4.4.6`、`@vue/test-utils@1.1.3`、`vue-jest@4.0.0`、`jest@26.6.3`、`babel-jest@26.6.3`）。`jest.config.js` 用 `preset: '@vue/cli-plugin-unit-jest'`。
  - 测试用例（`tests/unit/`，先写失败再修实现，红→绿 TDD 留档）：
    - `device-manage.spec.js`（7 例）：接入类型列渲染/筛选参数回传/syncGb28181Devices 调用/异常提示前缀/GB28181 编辑对话框含只读国标信息且无视频流输入框/reset 清理 device_type 与国标字段/RTSP 新增默认 device_type='RTSP'。
    - `api-device.spec.js`（3 例）：mock 掉 `utils/request`，断言 `syncGb28181Devices` 调 `/waring/device/gb28181/catalog/sync` 走 POST+`zlmServerId=1`，`refreshGb28181Status` 调 `/waring/device/gb28181/status/refresh` 走 POST+`zlmServerId=1`。
    - **结果**：`npm run test:unit` **10/10 通过**（先 3 失败，后修测试夹具：`resetForm` 来自 RuoYi 全局 mixin → 测试 localVue.mixin({ methods: { resetForm(){} } }) 兜底；el-dialog append-to-body → 断言用 `document.body.textContent`；同步异常文案加前缀便于断言）。
  - 关键坑：`@vue/cli-plugin-unit-jest@4.4.6` 配套 `vue-jest@4.0.0`（不要用 5.x，`vue-template-compiler@2.6.12` 解析 AST 失败）。`@vue/cli-plugin-unit-jest` 已安装到 WSL 构建副本 `/var/www/SVA-web/node_modules`；前端 `package-lock.json`（含 jest 条目）从 WSL 拷回 Windows 待提交。
  - `npm run build:prod` **成功**（dist 11M，nginx root 直指 `/var/www/SVA-web/dist`，即部署完成）。
  - `npm run lint` 仍命中 `.eslintignore` 忽略 src 的**既有基线门禁**（与阶段 1/2 记录一致）。
- 后端补丁（`SVA-backend` 阶段 3 收尾，与 SVA-web 同步提交）：
  - `Gb28181DeviceSyncServiceImpl.syncDevices`：增加空快照安全守卫——`if (channels == null) return new DeviceSyncResult();`（设备管理页"同步国标设备"按钮仅触发对账，不新增/不更新/不剔除任何通道；真正的目录下发必须携带权威快照）。
  - `Gb28181DeviceSyncServiceTest` 增至 7 例（新增 `syncDevicesWithoutSnapshotReturnsZeroesAndKeepsState`：seed 1 个 GB 通道，调 `syncDevices(1L, null)`，断言 created/updated/offlineMarked 全 0、catalog 0 次写、mirror 0 次更新）。
  - `mvn -pl ruoyi-admin -am test` **Tests run: 11**（含本次新增 1 例）全过；`backend.jar` 重新打包部署并重启 `easysva-backend`。
- 浏览器取证（10 张 PNG + 1 份 summary JSON + 可复现脚本存 `FWWsva/docs/evidence-phase3/`）：
  1. `p3-10-list-all` 设备管理列表 4 行，含"接入类型/媒体源"列；RTSP 行 "接入类型=RTSP 媒体源=直连 视频流地址=rtsp://..."，GB28181 行 "接入类型=国标 媒体源=平台 无视频流地址"。
  2. `p3-11-filter-gb28181` 接入类型下拉选 GB28181 → 列表共 2 条，RTSP 设备被过滤。
  3. `p3-12-sync-gb` 点"同步国标设备" → toast："国标目录对账完成，无变更：新增 0，更新 0，离线 0。等待平台推送目录或已有目录同步。"（空快照守卫生效，前端 UI 提示与后端零操作一致）。
  4. `p3-13-edit-gb28181` GB28181 行点修改 → 对话框含 "国标信息（目录同步获取，只读）" 区块（平台编码 340200000020000...、设备国标编码 34020000001320...、通道国标编码 3402000000131000001...、预览地址 ws://127.0.0.1:9992/live/dev_mock_camera_001.live.flv），**无视频流地址输入框**。
  5. `p3-14-edit-rtsp` RTSP 行点修改 → 对话框含"视频流地址"输入框（回归不受影响）。
  6. `p3-15-add-rtsp` 新增对话框 → 接入类型默认 "RTSP"、有视频流地址输入框、无国标信息区块。
  7. `p3-16-realtime` 实时监控页 1 行，列含"接入类型"tag。
  8. `p3-17-lixian` 离线设备页。
  9. `p3-18-preview-gb28181` 预览弹窗：播放器 video 元素已渲染、加载圈与控制条可见、正在拉流（画面未解码属 WSL2→Windows 端口转发限制，前后端接线由 p3-18+evidence-phase2/07-preview-online 双向证明）。
  10. `p3-03-admin-index` 登录后后台首页（含左侧菜单树）。
- 远程策略修订（`FWWsva` 阶段 3 同步更新）：`docs/superpowers/plans/2026-09-01-frontend-backend-acceptance.md` 阶段依赖与远程策略段追加"**推送前先拉取最新远程（避免冲突的强制步骤）**"——`git fetch origin <branch>` → 落后则 `git pull --rebase origin <branch>` → 冲突即停并人工裁决、禁止 `push --force` 覆盖他人提交。
- 提交与推送（按上述策略实际执行）：
  - `SVA-backend` 阶段 3 收尾提交 `feat: no-op GB28181 sync without catalog snapshot`（含 5 文件 +66/−8，HDeviceController/Gb28181DeviceSyncService/HDeviceService/Impl/Test），含空快照守卫与 1 例新测试。先 `git fetch origin` → 本地领先无落后 → 直接推送。
  - `SVA-web` 阶段 3 提交 `feat: adapt device UI for GB28181 sync`（含 api/device.js、views/device/{manage,realtime,lixian}.vue、views/deployment/add.vue、tests/unit/*、jest.config.js、package.json、package-lock.json）。
  - `FWWsva` 阶段 3 提交 `docs: record frontend-backend-acceptance phase-3 evidence`（含验收点2-4 文档阶段 3 章节、evidence-phase3/ 10 PNG + 1 summary + 7 脚本 + README、计划文档远程策略追加）。
  - 全部经本机 127.0.0.1:7890 代理推送 `origin/codex/frontend-backend-acceptance`。
- 遗留：浏览器侧 GB28181/RTSP 预览画面未解码（WSL2→Windows 端口转发限制）—阶段 5 整体联调前需加 WSL→Windows 端口转发或 `wsl --update` 启用镜像网络后补图；`npm run lint` 仍为既有基线门禁。

### 2026-09-02 阶段 4：睡岗告警与布控页面闭环交付记录

**执行时间：** 2026-09-02 16:55–18:54 +08:00（代码提交与推送 18:23 前后，E2E 取证 18:45–18:54）。

- **范围与目标**：不改变 AI 推理职责边界，保证睡岗事件从 Analyzer 回传到前端可查询、可筛选、可查看截图；告警页/布控页接入 `SLEEP_DUTY` 标准标识；既有 YOLO 告警零回归。
- **后端改动**（`SVA-backend` `codex/frontend-backend-acceptance`，在 d68dbba 之上新增补丁 commit `fix: surface device_type and device_id in alarm detail`）：
  - `ruoyi-admin/.../controller/HWaringController.java` 第 1249 行 `normalizeBehaviorType` 白名单补 `sleep_duty`（`d68dbba` 已推送），`SLEEP_DUTY_ALARM_TYPE/SLEEP_DUTY_ALARM_TYPE_NAME/SLEEP_DUTY_BEHAVIOR_TYPE` 常量与 `resolveAlarmTypeMeta` 分支此前已存在。
  - `ruoyi-admin/.../domain/HWaring.java` 增加 `private String device_type;`（阶段 4-2），列表 `selectWaringVo`（`HWaringMapper.xml` 第 140–142 行） `LEFT JOIN h_device hd ON hd.ape_id = hw.device_id` 输出 `hd.device_type AS device_type`（`d68dbba` 已推送）。
  - `ruoyi-admin/.../domain/Details.java` 增加 `private String device_id;` 与 `private String device_type;`（**阶段 4 补丁**）。
  - `ruoyi-admin/src/main/resources/mapper/waring/HWaringMapper.xml` `getOne`（第 737–820 行）SELECT 增加 `h_waring.device_id` 与 `hd.device_type AS device_type`，FROM 改为 `FROM h_waring LEFT JOIN h_device hd ON hd.ape_id = h_waring.device_id WHERE h_waring.w_id = #{w_id}`（**阶段 4 补丁**）—— 解决前端详情 `detailsInfo.device_type` 在 `d68dbba` 后仍取不到 `device_type` 的半成品点。
  - 测试：`HWaringAlarmTypeTest` 5 例（反射验证 `SLEEP_DUTY` 常量稳定性、`normalizeBehaviorType` 大小写不敏感与白名单拒未知、`resolveAlarmTypeMeta` 睡岗返回稳定 meta、未知行为返回 null、睡岗 meta 与缺席不同）通过；阶段 1–3 的全部测试同步 `mvn -pl ruoyi-admin -am test -DfailIfNoTests=false` 仍 11/11 全过；详情 SQL 改动由 API `04-04` 复验覆盖（无 MyBatis 异常、`device_id/device_type` 正确返回）。
- **前端改动**（`SVA-web` `de20f3b` `codex/frontend-backend-acceptance`）：
  - `src/views/warning/index.vue` 查询区新增「睡岗快捷筛选」按钮（`warning` plain 态；激活态 `warning` 实色 + 文案「睡岗告警（筛选中）」），点击调用 `applySleepDutyShortcut()` 设 `querySpecificParams.alarm_type_name='睡岗告警'` 并 `fetchData()`；点击激活按钮调用 `clearSleepDutyShortcut()` 清空。
  - 表格「报警类型」列改为 el-tag 渲染（新增 `getAlarmTypeTagType(row)`：`SLEEP_DUTY/睡岗告警`→warning、`SVA_ABSENCE/缺席/离岗`→danger、其他→info）。
  - 表格新增「设备类型」列（`GB28181`→success tag、`RTSP`→info tag、其他→原文/---）。
  - 详情 el-descriptions 加「设备类型」描述项（与表格列同 tag 渲染逻辑）。
  - `getBehaviorTypeLabel` 加 `if (behaviorType === 'sleep_duty') return '睡岗'; if (behaviorType === 'sleep') return '睡觉';`，对 AI 组规范 `sleep_duty` 标签与既有 `sleep` 双兼容。
  - 详情缺图占位：`<el-image>` 加载失败时显示「抓拍图加载失败」`<div slot="error">`，`picture_absolute_url` 为空时显示「暂无抓拍」；新增 `.snapshot-placeholder` 与 `.snapshot-placeholder-icon` 样式。
  - `src/views/deployment/add.vue` 第 1237–1257 行 `behaviorTypeOptions` 在 `sleep/睡觉` 之后插入 `{ value: 'sleep_duty', label: '睡岗' }`；第 1626 行 `normalizeBehaviorType` 白名单数组加 `'sleep_duty'`，复用既有 fallback 默认状态（不显示姿态/时长字段，符合「不在前端计算姿态或持续时间」）。
  - `tests/unit/warning-index.spec.js` 5 例全过（mock `@/api/warning` + `@/api/system/kanban` + `@/store`；localVue mixin 补 `resetForm`/`parseTime` 避免 RuoYi 全局 mixin 渲染报 `parseTime is not a function`）。`tests/unit/` 全量 15/15 通过。
  - `npm run build:prod`（WSL Node12）成功，`dist` 重建后 nginx root 立即生效（`/var/www/SVA-web/dist`），无需拷贝。
  - `npm run lint` 仍命中 `.eslintignore` 忽略 src 的既有基线门禁（与阶段 1/2/3 记录一致）。
- **E2E 取证**（5 张浏览器截图 + 8 份 API JSON 落 `FWWsva/docs/evidence-phase4/`，README.md 含完整复现命令与文件清单）：
  - 演示样本：注入 2 条 `SLEEP_DUTY` 告警（w_id 820 GB28181 主井口-国标01 带截图 / 821 RTSP 模拟摄像头 无图）；演示帧落盘 `/var/www/SVA-web/upload/alarm/sleep_duty_demo_20260902.jpg`（来自 `/opt/SVA/tmp/acceptance-person.jpg` 阶段 1 真实抓帧，HTTP 200）。
  - API：list total=81、`alarm_type_name=睡岗告警` 过滤 total=2、`waring/type/list` 含 `SLEEP_DUTY(t_id=41)`、详情 `device_id=gb-...01 device_type=GB28181 picture_absolute_url` 真值。
  - 浏览器：`p4-01` 全列表（报警类型 tag + 设备类型列 + 快捷筛选按钮）、`p4-02` 快捷筛选后仅睡岗 2 条（按钮切到「睡岗告警（筛选中）」warning 实色）、`p4-03` GB28181 详情（截图 `<img>` 加载成功 + 设备类型 GB28181 tag）、`p4-04` RTSP 详情（暂无抓拍占位 + 设备类型 RTSP tag）、`p4-05` 清除快捷筛选（按钮回 plain 态）。
- **关键坑与回退**：
  - **WSL 构建副本误用**：WSL 实际含阶段 4 代码的构建副本是 `/var/www/SVA-backend`，`/opt/SVA/SVA-backend` 仍是基线 `f3f9036`（无 `device_type` 字段）。首次打包从 `/opt/SVA/SVA-backend` 出发导致线上 MyBatis `Could not set property 'device_type'`，页面告警表 500；改用 `/var/www/SVA-backend` 重打包部署后 `04-01b/04-04b` API 与页面均恢复。已记入 2026-09-02 日志。
  - **详情 SQL 半成品点**：`d68dbba` 阶段 4 后端只补了 `selectWaringVo` 列表 JOIN，前端详情 `detailsInfo.device_type` 仍取空值；本阶段 4 补丁补 `Details.java` + `getOne` SQL 闭环。
- **提交与推送**（按远程策略：每次推送前 `git fetch` 确认无落后 → 推送；冲突即停）：
  - `SVA-backend` `d68dbba`（`feat: complete sleep-duty alarm business flow`，含 HWaringController 白名单修复 + HWaring.java/Mapper 测试 5 例，4 文件 +129/−2，**已推送** origin/codex/frontend-backend-acceptance，0 落后）。
  - `SVA-backend` 阶段 4 补丁 commit `fix: surface device_type and device_id in alarm detail`（Details.java + HWaringMapper.xml，2 文件 +5/−0）—— **本次一并推送**。
  - `SVA-web` `de20f3b`（`feat: complete sleep-duty alarm business flow`，含 warning/index.vue + deployment/add.vue + warning-index.spec 5 例，3 文件，**已推送** origin/codex/frontend-backend-acceptance，0 落后）。
  - `FWWsva` 本次提交 `docs: record frontend-backend-acceptance phase-4 evidence`（含本章节 + evidence-phase4/ 11 文件 + README + 5 PNG + 8 JSON + 5 脚本 + analyze.py）。
- **遗留**：`npm run lint` 与告警截图 404（历史 79 条 YOLO 告警的留存文件）仍为既有基线门禁；布控页 `sleep_duty` 选项由源码（`add.vue` 第 1242 行 `behaviorTypeOptions` 数组）作可复现证据，浏览器 E2E 因「添加算法 → 行为规则」嵌套表单未在 headless 流程中触发完整下拉，留待阶段 5 整体联调手测 + 后续单测覆盖。

### 2026-09-02 阶段 5：整体联调、回归和交付记录

**执行时间：** 2026-09-02 18:55–19:18 +08:00。

- **范围与目标**：在阶段 4 闭环基础上跑 5 条 E2E 场景（RTSP+睡岗、GB28181+睡岗、GB28181 离线/重连、两种设备布控启停、原 YOLO 回归），完成回归与构建（后端 `mvn test` / 前端 `lint`+`build:prod`），按远程策略三仓库提交并打验收标签 `acceptance-frontend-backend-v1`。

- **后端代码修复**（`SVA-backend` `codex/frontend-backend-acceptance`，在 `d68dbba` 之上新提交 `fix: resolve GB28181 device media stream for deployment start`，已落 Windows git 仓库 + WSL 同步副本 `/var/www/SVA-backend`）：

  - `ruoyi-admin/.../service/deployment/DeploymentAnalyzerClient.java` `addControl` 引入 `buildStreamUrl` 空值守卫（`if (StringUtils.isEmpty(streamUrl)) return AnalyzerResult.fail("设备无可播放媒体流…", "buildStreamUrl返回空: deviceId=… deviceType=…");`），重写 `buildStreamUrl(BindingConfig, apeId)`：对 `device_type='GB28181'` 的设备改为解析 `h_device.play_url`（ZLM 实际流名，如 `dev_mock_camera_001`）拼出 `rtsp://{zlmHost}:{zlmMediaRtspPort}/{mediaRef}`；RTSP 设备维持原 `rtsp://…/app/apeId` 协议不变。新增 `isGb28181Device`/`resolveDeviceType`/`extractZlmMediaRef`（去除 `.live.flv`/`.flv` 末尾）。
  - **根因**：原实现对所有设备都用 `ape_id` 当 ZLM 流名。GB28181 设备 `ape_id=gb-34020000001320000001-34020000001310000001` 在 ZLM 中并非有效流名——真实媒体流由 GB 信令通道产生，流名是 `gb28181_channel.play_url` 解析后的 `dev_mock_camera_001`，导致 analyzer 拉 RTSP 失败 `pull stream connect error`、GB 布控 start 一直无法 `RUNNING`。
  - **验证**：`mvn -pl ruoyi-admin -am package -DskipTests` 成功（jar 105904105B），`/opt/SVA/backend/backend.jar` 重新部署，重启 `easysva-backend`，GB 布控 `addControl` 返回 `success=true`、DB `deployment_task.status='RUNNING'`、`updated_at=2026-09-02 19:12:02`。

- **场景 1/2/5：ws 链路告警注入（RTSP+睡岗 / GB28181+睡岗 / 原 YOLO 停留回归）**

  - 准备：`/var/www/SVA-web/upload/alarm/p5-sleep-rtsp-20260903.jpg`、`p5-sleep-gb-20260903.jpg`、`p5-yolo-dwell-20260903.jpg`（从 `/opt/SVA/tmp/acceptance-person.jpg` 拷贝，nginx `/alarm/` 路径 HTTP 200）。
  - 事件脚本：`FWWsva/docs/evidence-phase5/phase5_ws_push.py`（WSL 内 `apt install python3-websocket`，读 `events-s1s2s5.json` 逐条推 `ws://127.0.0.1:9114/websocket/sva/noop`，自动填 `startTimestampMs/timestampMs`）。
  - 9 条 `detect.event` 推送：RTSP sleep_duty（30/45/60s 各 1 条，控制 ID `controlDrwhlzWGKy6DaS`）+ GB28181 sleep_duty（25/40/55s，控制 ID `controlBVwDyFaFSi7WyW`）+ YOLO dwell（15/30/45s，任务 `task_yolo_dwell_001`）。
  - DB 落库验证（`SELECT w_id, w_name, behavior_type, device_name, device_type, duration_ms, picture_absolute_url FROM h_waring JOIN h_device ON h_device.ape_id=h_waring.device_id WHERE w_id IN (...)`）：3 条 canonical 告警 `ev-p5-rtsp-sleep-a-20260903`（`SLEEP_DUTY/睡岗告警/sleep_duty/模拟RTSP摄像头/RTSP/60000ms`）、`ev-p5-gb-sleep-a-20260903`（`主井口-国标01/GB28181/55000ms`）、`ev-p5-yolo-dwell-a-20260903`（`SVA_DWELL/停留告警/dwell/模拟RTSP摄像头/RTSP/45000ms`），三条 `picture_absolute_url` 全部 200。

- **场景 3：GB28181 离线/重连双向验证**

  - 离线态：调 `/waring/device/gb28181/status/refresh` → `{unavailable:1, available:1}`；`/waring/device/preview?deviceId=gb-…-10000002` 返回 500「国标设备当前离线，无法预览」。
  - 推送 ghost 流：`ffmpeg -f lavfi -re -i color=...` 推流到 `rtsp://127.0.0.1:9994/live/gb-ghost-cam-0002`；再调 refresh → `{unavailable:0, available:2}`；preview 返回 `playUrl`；DB `h_device.is_online=1/media_online=1/last_media_seen_at=2026-09-02 19:09:01`。
  - 断流：kill ffmpeg 进程；refresh → `{unavailable:1, available:1}`；DB `is_online=2/media_online=0/last_media_seen_at=NULL`。
  - 定时器自愈：`@Scheduled(fixedDelayString="${easysva.gb28181.status-refresh-ms:60000}")` 在 push/断流 60s 周期内自动把状态翻到正确值，无需人工触发。

- **场景 4：两种设备布控启停**

  - 创建：`POST /deployment/create` 用 `req-rtsp-sleep.json`/`req-gb-sleep.json`（taskName `P5-RTSP-睡岗布控` / `P5-GB28181-睡岗布控`，`deviceId=dev_mock_camera_001` / `gb-34020000001320000001-34020000001310000001`，`algorithmTasks=[on_yolo11n_80+person]`，`geometryConfig.behaviorRules` 含 `{behaviorType:'sleep_duty'}`，recordEngine A-SERVER，pushEnabled false）。
  - start：调 `/deployment/start` 触发 `DeploymentAnalyzerClient.addControl`，analyzer HTTP 200 `success=true`，DB 两条 `status='RUNNING'`、`updated_at=2026-09-02 19:12:01/19:12:02`。RTSP 任务 `controlDrwhlzWGKy6DaS`、GB28181 任务 `controlBVwDyFaFSi7WyW`。
  - 已知缺口（`analyzer 控制态不持久化`）：easysva-analyzer 在后端 jar 重部署期间（19:09 附近）重启，内存控制表清空；DB 仍记 `RUNNING` 但 analyzer 报 `there is no such control` 导致 stop 失败。处置：再次 start 两任务恢复 RUNNING。**根因**：analyzer 内存控制态无持久化，与 DB 状态不一致；建议后续改造方向——analyzer 控制表持久化（DB/Redis），或后端重启时联动 analyzer 重启后从 DB 重建控制。已记入 2026-09-02 日志与 README 故障处理段。

- **UI 取证**（8 张 PNG + 1 份 summary 存 `FWWsva/docs/evidence-phase5/`，脚本 `run-phase5-ui.js` 可复现）：

  - `p5-01-alarm-list-all-20260902-191730.png` 报警列表全量 5 行（最新置顶 1 条停留告警 YOLO 回归 + 2 条 ws 回传睡岗 GB28181/RTSP + 2 条阶段 4 历史）。
  - `p5-02-sleep-filter-20260902-191730.png` 睡岗快捷筛选 4 行（GB28181+RTSP+2 条历史）。
  - `p5-03-sleep-detail-1st-20260902-191730.png` 第 1 条详情：设备类型 `GB28181` tag、`<img>` 加载成功、规则 ID `rule-sleep-gb-a`、区域 `full-frame`、事件阶段「结束」、持续 55 秒、`AI复核状态: 未复核`。
  - `p5-04-sleep-detail-2nd-20260902-191730.png` 第 2 条详情：设备类型 `RTSP` tag、`<img>` 加载成功、规则 ID `rule-sleep-rtsp-a`、持续 1 分。
  - `p5-05-alarm-list-dwell-regression-20260902-191730.png` 清除快捷筛选后全量 5 行，原 YOLO `停留告警` 仍在第 1 行（行为类型=停留、设备类型=RTSP、复核失败占位）。
  - `p5-06-dwell-detail-20260902-191730.png` 停留告警详情（YOLO 回归）：抓拍图来自阶段 4 历史截图，行为类型 `dwell`、规则 `rule-dwell-yolo-1`、区域 `full-frame`、事件阶段「结束」、持续 45 秒。
  - `p5-07-deploy-list-20260902-191730.png` 布控列表 3 行 RUNNING：`controlBVwDyFaFSi7WyW (P5-GB28181-睡岗布控 / person-yolo11n80 / 30s / 19:12:02)` + `controlDrwhlzWGKy6DaS (P5-RTSP-睡岗布控 / person-yolo11n80 / 30s / 19:12:01)` + `task_yolo_dwell_001 (验收点1-YOLO模拟检测 / yolo11n_80类目标检测 / 180s / 09-01 16:50:06)`。
  - `p5-08-lixian-gb-offline-20260902-191730.png` 离线设备页 1 行：副井口-国标02(幽灵流) `gb-34020000001320000001-34020000001310000002` 状态「离线/停用」（与场景 3 离线态一致，幽灵流已断）。
  - `p5-summary-20260902-191730.json` 含每张截图 `name/file/note/url` 与完整菜单 dump（"首页"… "布控管理"/"布控列表" 等 35 项）。

- **回归与构建**：

  - **后端**：`mvn -pl ruoyi-admin -am test -DskipITs -q` 在 WSL `/var/www/SVA-backend` 跑出 `Tests run: 17, Failures: 0, Errors: 0, Skipped: 0`（`HDeviceControllerTest` 3 例 + `HDeviceServiceImplTest` 1 例 + `Gb28181DeviceSyncServiceTest` 8 例 + `HWaringAlarmTypeTest` 5 例）；`HDeviceControllerTest`/`HDeviceServiceImplTest` 沿用既有 1–3 阶段 fixture，阶段 4 修复未引入回归。`mvn package` 重建 `backend.jar` 部署。
  - **前端**：`npm run build:prod`（Windows Node 22 + `NODE_OPTIONS=--openssl-legacy-provider`）成功生成 `dist/`（`app.92135993.css` 269.90 KiB、`app.*.js` 主块 ~1MB，gzipped 体积合理），nginx 立即指向 `/var/www/SVA-web/dist` 部署生效。
  - **lint**：`npm run lint` 仍命中 `.eslintignore` 忽略 src 的既有基线门禁（与阶段 1/2/3/4 记录一致）。针对性 lint 阶段 3/4 改动的 7 个文件（`src/api/device.js`、`views/deployment/add.vue`、`views/device/{index,lixian,manage,realtime}.vue`、`views/warning/index.vue`）无业务错误（46 errors 全是 `object-curly-spacing`/`prefer-const`/`spaced-comment` 风格问题，0 个 `no-undef`/`no-unused-vars` 真实逻辑错误）。
  - **服务复验**：WSL `ps -ef` 验证 7 个核心进程均 active：MediaServer (PID 178) / ffmpeg RTSP 模拟 (PID 179) / redis (PID 183) / mariadb (PID 277) / nginx (297+12 workers) / backend.jar (PID 11270) / Analyzer-gpu (PID 11272)。`POST /login` → token 293 位、`{"code":200,"msg":"操作成功"}`。
  - **DB 状态**：`h_device=4 / gb28181_channel=2 / h_waring=84 / h_waring_type=1 (SLEEP_DUTY) / deployment_task=3 / deployment_task_algorithm=3`。

- **关键坑与回退**：

  - **GB28181 布控 `pull stream connect error`**：根因 `DeploymentAnalyzerClient.buildStreamUrl` 用 `ape_id` 当 ZLM 流名，对 GB28181 设备 `ape_id="gb-…-10000001"` 永远在 ZLM `getMediaList` 中找不到。修复：按 `device_type` 分流，GB28181 走 `play_url` 解析路径（详见「后端代码修复」段）。**该 bug 影响所有 GB28181 设备的布控启动**，阶段 5 E2E 才发现并修复，已在本次提交。
  - **analyzer 控制态不持久化**（集成缺口）：详见场景 4 段，处置后再 start 恢复 RUNNING，建议后续改造。
  - **浏览器预览未闭环（阶段 5 当时的状态）**：GB28181/RTSP 预览 video 元素、加载圈与控制条可见，但未取得解码画面证据。WSL2→Windows 端口可达性是待排查方向，不能仅凭加载圈认定根因或证明播放成功。

- **演示样本与 ws 客户端**（`FWWsva/docs/evidence-phase5/`）：`smoke-create-req.json`、`req-rtsp-sleep.json`、`req-gb-sleep.json`、`phase5_ws_push.py`（WSL 内 `python3 -m pip install websocket-client`，已在 `/opt/SVA/tmp/` 跑过）、`events-s1s2s5.json`（9 条事件）、`run-phase5-ui.js`（playwright-core headless + Chromium 152，NODE_PATH 指 `.p3tools/node_modules`，`C:/Users/19904/.agent-browser/browsers/chrome-152.0.7977.75/chrome.exe`）。

- **远程提交与标签状态（已完成）**：

  - `SVA-backend` 已提交：`fix: resolve GB28181 device media stream for deployment start`（`DeploymentAnalyzerClient.java`，含 `addControl` 空值守卫 + `buildStreamUrl` 重写 + 3 个 helper）。
  - `SVA-web` 阶段 5 期间无代码改动（仅构建产物 `dist/` 在部署路径，不入 git）。
  - `FWWsva` 已提交：`docs: complete acceptance 2-4 delivery evidence`（本章节、`evidence-phase5/` 与 README）。
  - 三仓库的 `master` 已推送，且 `acceptance-frontend-backend-v1` 标签已创建并推送；后续改动按正常 `fetch`、必要时 `pull --rebase`、冲突即停的流程处理。

- **阶段 5 当时的遗留**：① lint 与历史告警图片 404；② Analyzer 重启后控制态丢失，数据库仍为 RUNNING 时停止失败；③ 浏览器实际解码与真实国标协议链路需要独立复验；④ 演示样本不是缺失历史截图的替代品。以上为历史记录，本轮代码与验证结果另行记录。

### 2026-09-03 历史图片对账与环境复核

- 新增 `deploy/scripts/reconcile-alarm-images.sh`，默认 dry-run，只有显式 `--apply` 才更新确实存在、非空且未越过 `alarm/` 目录的普通文件所对应 URL。正确 URL 不更新，原图缺失只报告，不修改数据库、不伪造截图。
- 回归脚本 `bash deploy/tests/reconcile-alarm-images-test.sh` 已通过，覆盖默认无写入、现存文件规范化、缺失文件、空 URL 列、跨根符号链接、目录误判、重复更新、恶意站点参数、SELECT/UPDATE 失败和多余参数拒绝。
- 本机数据库只读对账：`selected=63 eligible=3 missing=59 unsafe=0 unchanged=1 updated=0 conflicted=0`。这次没有执行 `--apply`；3 条可修复记录为现存演示图片，59 条缺失历史文件仍不能通过修改 URL 恢复。此快照与阶段 5 的行数不同，不将其混写为同一次验收结果。
- 2026-09-03 本机 `easysva-wvp`、`easysva-gb-media` 没有已安装的 unit；核心后端、媒体、Analyzer、nginx 服务正常。已有 GB28181 目录绑定模拟 RTSP 媒体的业务证据，不等于真实设备 SIP/INVITE/RTP 链路通过；仍需真实链路及浏览器解码证据。
- 运维操作、备份和回滚限制见 [README](../README.md#历史告警图片对账) 与 [部署说明](验收点1-部署与架构说明.md)。

### 2026-09-03 验收阻塞项修复与合并（最终）

在上述对账之后，`--apply` 已执行：`selected=63 eligible=3 missing=59 unsafe=0 unchanged=1 updated=3 conflicted=0`，3 条可修复 URL（`alarm/p5-sleep-rtsp-20260903.jpg`、`alarm/p5-sleep-gb-20260903.jpg`、`alarm/p5-yolo-dwell-20260903.jpg`）已规范化，59 条缺失历史文件保持不变（保留 UI 占位图，不伪造数据）。

**前端（`SVA-web`，提交 `0b293e9`，已合入 `master`）：**

- lint gate 恢复：`.eslintignore` 不再忽略 `src`；`npx eslint --ext .js,.vue src` 从基线 26266 errors 清零（现 0 errors / 48 warnings）。修复内容：3 个真实 `no-undef`（算法页 `showModal`/`UploadDialog` 引用、`monitoring-points.vue` 未被调用的 `getData()` 死代码）、72 处 `eqeqeq`、逗号运算符语句、`setTimeout` 字符串隐式 eval、`new Array()`、`state.hasOwnProperty` 直调、prop 默认值工厂等；对源自若依 Crontab 组件的带标签循环与 computed 回写保留行为并加文件级豁免注释。
- RTSPPlayer 移除硬编码回退：`playUrl` 为空时渲染不可用态，不再回退 `ws://192.168.125.30:9117`，由 `tests/unit/rtsp-player.spec.js` 覆盖。
- `npm run test:unit`：6 套件 / 19 用例全部通过；`npm run build:prod` 构建成功（Node 22 需 `NODE_OPTIONS=--openssl-legacy-provider`，webpack4 工具链限制，非代码问题）。

**后端（`SVA-backend`，提交 `e3665b2` + `bd242f2`，已在 `master`）：**

- 取消幂等（TDD）：`DeploymentAnalyzerClient` 在 `action=cancel` 且 Analyzer 返回 `code=0, msg="there is no such control"` 时映射为幂等成功「停止成功，布控已不存在」，`DeploymentAnalyzerClientTest` 以 mock `RestTemplate` 覆盖该分支。
- JUnit 4→5 迁移：Spring Boot 4 不再带 vintage 引擎，`HDeviceControllerTest`、`Gb28181DeviceSyncServiceTest`、`HWaringAlarmTypeTest` 3 个类 16 个用例此前被静默跳过，已迁移至 Jupiter。`mvn -pl ruoyi-admin -am test -DskipITs` 现为 **36 用例全绿**（7 个测试类全部执行）。
- 重建 `backend.jar` 部署并重启 `easysva-backend`，端到端验证：对 Analyzer 控制已不存在的任务 `controlDrwhlzWGKy6DaS` 连续两次 `POST /deployments/{id}/stop` 均返回 `code=200, success=true`（幂等），`deployment_task.status` 由 `RUNNING` 收敛为 `STOPPED`。

**远程状态：** 三个仓库均已提交至本地 `master`：`SVA-backend` = `e3665b2`（取消幂等）+ `bd242f2`（JUnit 5 迁移），`SVA-web` = `0b293e9`（RTSPPlayer + lint gate），`FWWsva` = 本文档提交（`docs: document acceptance recovery and image reconciliation`）。

### 2026-09-03 补充复验：图片 URL、RTSP 实流与预览链路

- **对账修复 URL 复验通过**：`h_waring` 中 `w_id=822/823/824` 的 `picture_url` 已规范为 `alarm/p5-*.jpg`，三个公网 URL 经 nginx 访问均返回 HTTP 200（Task 4「recoverable URLs return HTTP 200」复验项闭环）。
- **RTSP 实流可解码证据**：在 WSL 内 `ffmpeg -rtsp_transport tcp -i rtsp://127.0.0.1:9994/live/dev_mock_camera_001 -frames:v 1` 成功抓取单帧（64 KB JPEG，画面为模拟器推流的街景行人），归档为 `docs/evidence-phase5/rtsp-frame-verify-20260903.jpg`。媒体链路（模拟器→ZLMediaKit→RTSP 拉流→解码）在 WSL 内全通。
- **布控生命周期端到端复验**：创建 `VERIFY-PREVIEW-0903`（RTSP，`dev_mock_camera_001`）→ `POST /deployments/{id}/start` 返回 Analyzer `code=1000 "add success"`（控制真实注册）→ `POST /{id}/stop` 返回「停止成功，布控已取消」（真实取消，非幂等分支）→ DB 状态 `STOPPED`。与上午对「控制已不存在」任务的停止（幂等分支）合起来，取消路径的两个分支都有运行时证据。
- **新发现跨组集成缺口（记录，不在本组修复范围）**：`POST /deployments/{id}/live-output` 失败，后端正确调用 `POST http://127.0.0.1:9993/api/control/live-output`，Analyzer 返回 404——该端点在 Analyzer（SVA-server 组，C++）侧未实现。后端契约与错误透传正常；需要 SVA-server 组补充实现后，浏览器预览才能拿到服务端权威的 `algorithmStreamUrl`。另：WVP（18080）未部署，GB28181 同步定时任务持续报 `Connection refused`，与既有记录一致。
- **运维注意（WSL2）**：本机 WSL 虚拟机空闲超时很短，调用间隔稍长即整机冷启动（/tmp 清空、全部 systemd 服务重新拉起、Analyzer 内存控制态丢失）。长时间演示/验收前建议先保活（如 `wsl.exe -d Ubuntu-22.04-easySVA sleep 900` 后台运行），否则「Analyzer 重启后控制丢失」会频繁复现——该场景正是本次取消幂等修复所覆盖的。
